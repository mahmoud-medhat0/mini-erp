// The token map (CSS var() strings / arrays) is consumed by tailwind.config.ts
// theme.extend. Typed as any so it satisfies Tailwind's nested theme types.
declare const tokens: any;
export default tokens;
